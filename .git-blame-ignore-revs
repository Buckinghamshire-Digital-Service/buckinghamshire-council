# This file contains all commits that should be ignored when using `git blame`.
# This is helpful for cases like a commits that were purely formatting changes.
# e.g. as part of a formatter upgrade
# Reference: https://michaelheap.com/git-ignore-rev/
#
# Each line should contain one commit hash.
# You can add comments before each commit hash with the pound symbol (#).
